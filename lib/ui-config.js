/**
 * Configuración de estilos visuales globales
 */
export const UI_CONFIG = {
  // Transparencia de las tarjetas (Timeline, ResponseCard)
  // bg-card/X backdrop-blur-Y
  card: {
    opacity: "bg-card/60",
    blur: "backdrop-blur-lg",
  },
  // Transparencia del Header
  header: {
    opacity: "bg-background/40",
    blur: "backdrop-blur-xl",
  },
  // Visibilidad del fondo animado
  background: {
    opacity: "opacity-60",
    blobOpacity: "10", // bg-primary/X
  },
};
